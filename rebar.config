{erl_opts, [debug_info]}.

{deps, []}.

{shell, [{apps, [demo]}]}.

{plugins, [steamroller]}.

{relx, [{release, {demo_rel, "0.0.1"},
         [demo, crypto]},

    {dev_mode, false},
        {include_erts, true},
        {include_src, true},
        {extended_start_script, false}]}.
